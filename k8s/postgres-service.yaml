apiVersion : v1
kind : Service
metadata :
 name : postgres-service
 namespace: dev
 labels :
  tier : postgres-service
spec :
 selector :
  app : postgres-app
 type: ClusterIP
 ports:
  - port: 5432
    targetPort: 5432
