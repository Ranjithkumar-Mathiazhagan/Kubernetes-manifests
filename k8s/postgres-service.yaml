apiVersion : v1
kind : Service
metadata :
 name : postgres-service
 labels :
  tier : postgres-service
spec :
 selector :
  app : postgres-app
 type: ClusterIP
 ports:
  - port: 5432
    targetPort: 5432