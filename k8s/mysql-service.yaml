apiVersion : v1
kind : Service
metadata :
 name : mysql-service
 namespace: dev
 labels :
  tier : mysql-service
spec :
 selector :
  app : mysql-app
 type: ClusterIP
 ports:
  - port: 3306
    targetPort: 3306
  
